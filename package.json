{
  "name": "agent-chrome-test",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic Chrome testing extension - MCP + WebSocket bridge for AI agents to test in Chrome",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean"
  }
}
